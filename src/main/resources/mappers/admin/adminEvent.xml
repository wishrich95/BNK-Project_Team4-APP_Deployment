<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 게시판 이벤트 -->
<mapper namespace="kr.co.busanbank.mapper.AdminEventMapper">

    <sql id="searchWhere">
        <trim prefix="" prefixOverrides="AND |OR ">
            <if test="pageRequestDTO.searchType != null and pageRequestDTO.keyword != ''">
                <choose>
                    <when test="pageRequestDTO.searchType == 'title'">
                        AND TITLE LIKE '%' || #{pageRequestDTO.keyword} || '%'
                    </when>
                </choose>
            </if>
        </trim>
    </sql>

    <select id="findById" resultType="kr.co.busanbank.dto.BoardDTO">
        SELECT
        id,
        boardtype AS boardType,
        title,
        content,
        boardfile AS "file",
        createdat AS createdAt,
        updatedat AS updatedAt,
        status FROM BOARD
        WHERE ID = #{id} AND BOARDTYPE = 'event'
    </select>

    <select id="findAll" resultType="kr.co.busanbank.dto.BoardDTO">
        SELECT
        id,
        boardtype AS boardType,
        title,
        boardfile AS "file",
        createdat AS createdAt,
        updatedat AS updatedAt,
        status FROM BOARD
        WHERE BOARDTYPE = 'event'
        ORDER BY createdat DESC
        OFFSET #{pageRequestDTO.offset} ROWS
        FETCH NEXT #{pageRequestDTO.size} ROWS ONLY
    </select>
    <select id="selectCount" resultType="int">
        SELECT COUNT(*) FROM BOARD
        WHERE BOARDTYPE = 'event'
    </select>

    <select id="searchAll" resultType="kr.co.busanbank.dto.BoardDTO">
        SELECT * FROM BOARD
        <where>
            BOARDTYPE = 'event'
            <if test="pageRequestDTO.searchType != null and pageRequestDTO.keyword != ''">
                AND <include refid="searchWhere"/>
            </if>
        </where>
        ORDER BY ID DESC
        OFFSET #{pageRequestDTO.offset} ROWS
        FETCH NEXT #{pageRequestDTO.size} ROWS ONLY
    </select>
    <select id="searchCountTotal" resultType="int">
        SELECT COUNT(*)
        FROM BOARD
        <where>
            BOARDTYPE = 'event'
            <if test="pageRequestDTO.searchType != null and pageRequestDTO.keyword != ''">
                AND <include refid="searchWhere"/>
            </if>
        </where>
    </select>

    <insert id="insertEvent" parameterType="kr.co.busanbank.dto.BoardDTO">
        INSERT INTO BOARD (BOARDTYPE, TITLE, BOARDFILE)
        values (#{boardType}, #{title} , #{file})
    </insert>

    <update id="modifyEvent">
        UPDATE BOARD SET
        TITLE = #{title}, BOARDFILE = #{file}, UPDATEDAT = (SYSTIMESTAMP AT TIME ZONE 'Asia/Seoul')
        WHERE ID = #{id} AND BOARDTYPE = 'event'
    </update>

    <delete id="singleDelete">
        DELETE FROM BOARD
        WHERE ID = #{id} AND BOARDTYPE = 'event'
    </delete>
    <delete id="delete" parameterType="list">
        DELETE FROM BOARD
        WHERE ID IN
        <foreach item="id" collection="list" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND BOARDTYPE = 'event'
    </delete>
</mapper>