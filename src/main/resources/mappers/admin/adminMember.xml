<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 고객센터 회원 목록 -->
<mapper namespace="kr.co.busanbank.mapper.AdminMemberMapper">

    <select id="findAll" resultType="kr.co.busanbank.dto.AdminMemberDTO">
        SELECT
        u.USERNO,
        u.USERNAME,
        u.USERID,
        u.HP,
        u.ROLE,
        COUNT(a.ACCOUNTNO) AS accountNo,
        SUM(a.BALANCE) AS balance,
        u.UPDATEDAT,
        u.STATUS,
        u.REGDATE
        FROM USERS u
        JOIN ACCOUNT a ON u.USERNO = a.USERID
        GROUP BY
        u.USERNO,
        u.USERNAME,
        u.USERID,
        u.HP,
        u.ROLE,
        u.UPDATEDAT,
        u.STATUS,
        u.REGDATE
        ORDER BY u.USERNO DESC
        OFFSET #{pageRequestDTO.offset} ROWS
        FETCH NEXT #{pageRequestDTO.size} ROWS ONLY
    </select>
    <select id="selectCount" resultType="int">
        SELECT COUNT(DISTINCT u.USERNO) FROM USERS u
        JOIN ACCOUNT a ON u.USERNO = a.USERID
    </select>

    <select id="findByUserNo" resultType="kr.co.busanbank.dto.AdminMemberDTO">
        SELECT
        u.USERNO,
        u.USERNAME,
        u.USERID,
        u.HP,
        u.ROLE,
        COUNT(a.ACCOUNTNO) AS accountNo,
        SUM(a.BALANCE) AS balance,
        u.UPDATEDAT,
        u.STATUS,
        u.REGDATE
        FROM USERS u
        JOIN ACCOUNT a ON u.USERNO = a.USERID
        WHERE USERNO = #{userNo}
        GROUP BY
        u.USERNO,
        u.USERNAME,
        u.USERID,
        u.HP,
        u.ROLE,
        u.UPDATEDAT,
        u.STATUS,
        u.REGDATE
    </select>
</mapper>