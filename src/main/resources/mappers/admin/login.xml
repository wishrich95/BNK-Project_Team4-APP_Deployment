<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.busanbank.mapper.AdminLoginMapper">
    <select id="findByLoginId" parameterType="string" resultType="kr.co.busanbank.dto.AdminDTO">
        SELECT * FROM ADMIN WHERE loginId = #{loginId}
    </select>

</mapper>